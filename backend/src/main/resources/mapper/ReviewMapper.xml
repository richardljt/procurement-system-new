<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.procurement.mapper.ReviewMapper">

    <insert id="insertMeetingMaterial">
        INSERT INTO meeting_material (meeting_id, file_name, file_path, file_size, uploader_id, uploader_name, upload_time)
        VALUES (#{meetingId}, #{fileName}, #{filePath}, #{fileSize}, #{uploaderId}, #{uploaderName}, NOW())
    </insert>

    <insert id="insertMeetingQuestion">
        INSERT INTO meeting_question (meeting_id, content, asker_id, asker_name, is_anonymous, status, attachment_name, attachment_path, create_time)
        VALUES (#{meetingId}, #{content}, #{askerId}, #{askerName}, #{isAnonymous}, #{status}, #{attachmentName}, #{attachmentPath}, NOW())
    </insert>

    <insert id="insertMeetingReply">
        INSERT INTO meeting_reply (question_id, content, replier_id, replier_name, attachment_name, attachment_path, create_time)
        VALUES (#{questionId}, #{content}, #{replierId}, #{replierName}, #{attachmentName}, #{attachmentPath}, NOW())
    </insert>

    <insert id="insertMeetingVote">
        INSERT INTO meeting_vote (meeting_id, round, voter_id, voter_name, score, comment, create_time)
        VALUES (#{meetingId}, #{round}, #{voterId}, #{voterName}, #{score}, #{comment}, NOW())
    </insert>

    <insert id="insertReviewLog">
        INSERT INTO review_log (meeting_id, operator_id, operator_name, action_type, detail, create_time)
        VALUES (#{meetingId}, #{operatorId}, #{operatorName}, #{actionType}, #{detail}, NOW())
    </insert>

</mapper>
