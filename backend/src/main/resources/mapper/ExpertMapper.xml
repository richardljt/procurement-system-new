<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.procurement.mapper.ExpertMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="expertId">
        INSERT INTO expert (name, department, entry_date, industries, level, avatar, user_id, create_user_id, create_user_name, create_time, update_time)
        VALUES (#{name}, #{department}, #{entryDate}, #{industries}, #{level}, #{avatar}, #{userId}, #{createUserId}, #{createUserName}, NOW(), NOW())
    </insert>

    <update id="update">
        UPDATE expert SET name=#{name}, department=#{department}, entry_date=#{entryDate}, industries=#{industries},
                          level=#{level}, avatar=#{avatar}, user_id=#{userId}, update_time=NOW()
        WHERE expert_id = #{expertId}
    </update>

</mapper>
